<component name="ArtifactManager">
  <artifact type="war" name="hazard_zones:war">
    <output-path>$PROJECT_DIR$/target</output-path>
    <properties id="maven-jee-properties">
      <options>
        <module>hazard_zones</module>
        <packaging>war</packaging>
      </options>
    </properties>
    <root id="archive" name="hazard_zones.war">
      <element id="artifact" artifact-name="hazard_zones:war exploded" />
    </root>
  </artifact>
</component>